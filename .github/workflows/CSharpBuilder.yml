
on:
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:
    
jobs:
  restore-build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      working-directory: ./srcCSharp/CraneSim
      run: dotnet restore
    - name: Build
      working-directory: ./src/CSharp/CraneSim
      run: dotnet build --no-restore
    - name: Run all tests
      working-directory: ./srcCSharp/CraneSim
      run: dotnet test --no-build --verbosity normal
